# this is a comment, working v well 10/10
# hello

# function import test
summon sin x

func fib x:
  if 3 > x:
    1
  else
    fib(x - 1) + fib(x - 2)
  end
end

func main:
  using a = 10, b = 2, c = 0:
    for i = 1, i < a, b:
      fib(i)
    end
  end
end
